<div class="divclass">
  <div class="wrapper">
    <div class="forbidden-container">
      <%= image_tag 'instinct_logo.png', style: "height: 100px; margin-bottom: 30px;" %>
      
      <h1 class="forbidden-title">Access Denied</h1>
      
      <p class="forbidden-message">
        You don't have permission to access this area.
      </p>
      
      <p class="forbidden-info">
        This section is restricted to administrators only. 
        If you believe you should have access, please contact your system administrator.
      </p>
      
      <div class="forbidden-actions">
        <% if spree_current_user %>
          <%= link_to "Return to Home", current_store.url, class: 'btn btn-primary', target: '_blank' %>
          <%= link_to "Sign Out", spree.logout_path, class: 'btn btn-secondary' %>
        <% else %>
          <%= link_to "Sign In", spree_login_path, class: 'btn btn-primary' %>
        <% end %>
      </div>
    </div>
  </div>
</div>

<style type="text/css">
  html, body {
    margin: 0 !important;
    padding: 0 !important;
    height: 100% !important;
    overflow-x: hidden !important;
  }

  #content {
    padding: 0 !important;
    margin: 0 !important;
  }

  .divclass {
    font-family: "Open Sans", sans-serif;
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    margin: 0 auto;
    padding: 0;
    z-index: 1;
  }

  .wrapper {
    display: flex;
    align-items: center;
    flex-direction: column;
    justify-content: center;
    width: auto;
    height: 100vh;
    min-height: 100vh;
    padding: 20px;
    padding-bottom: 80px;
    margin: 0 auto;
    background: rgba(33, 150, 243, 0.85);
    font-family: "Open Sans", sans-serif;
    background-image: url("https://i.imgur.com/yzS1Nse.jpeg");
    background-size: cover;
    background-position: center center;
    background-repeat: no-repeat;
    background-attachment: fixed;
  }

  .footer-spree-copyright {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    z-index: 2;
    background: rgba(0, 0, 0, 0.5);
    border: 0 !important;
  }

  .footer-spree-copyright * {
    color: white !important;
  }

  .footer-spree-copyright a:hover {
    opacity: 0.8;
  }

  .forbidden-container {
    border-radius: 5px;
    padding: 40px 30px;
    width: 90%;
    max-width: 500px;
    background: #ffffff;
    text-align: center;
    box-shadow: 0px 2px 10px rgba(0,0,0,0.3);
  }

  .forbidden-title {
    color: #d32f2f;
    font-size: 32px;
    font-weight: 600;
    margin-bottom: 20px;
  }

  .forbidden-message {
    color: #333;
    font-size: 18px;
    margin-bottom: 15px;
    font-weight: 500;
  }

  .forbidden-info {
    color: #666;
    font-size: 14px;
    margin-bottom: 30px;
    line-height: 1.6;
  }

  .forbidden-actions {
    display: flex;
    gap: 15px;
    justify-content: center;
    flex-wrap: wrap;
  }

  .forbidden-actions .btn {
    padding: 12px 30px;
    font-size: 16px;
    border-radius: 4px;
    text-decoration: none;
    border: none;
    cursor: pointer;
    transition: all 0.3s ease;
    display: inline-block;
  }

  .forbidden-actions .btn-primary {
    background: #2196F3;
    color: white;
  }

  .forbidden-actions .btn-primary:hover {
    background: #1976D2;
  }

  .forbidden-actions .btn-secondary {
    background: #757575;
    color: white;
  }

  .forbidden-actions .btn-secondary:hover {
    background: #616161;
  }
</style>
