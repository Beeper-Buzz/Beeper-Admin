<div class="divclass">
  <div class="wrapper">
    <h3 class="spree-mb-large spree-mt-large spree-header" style="color: aliceblue;"><%= image_tag 'instinct_logo.png', :style => "height: 100px;" %></h3>
    <%= form_for Spree.user_class.new, as: :spree_user, url: spree_login_path do |f| %>
      <div id="password-credentials" class="mb-4">
        <div class="form-group mb-4">
          <p class="form-group checkout-content-inner-field">
            <%= f.email_field :email, class: 'box1 spree-flat-input', placeholder: Spree.t(:email), value: params.dig(:spree_user, :email) %>
          </p>
        </div>
        <div class="form-group mb-4">
          <p class="form-group checkout-content-inner-field">
            <%= f.password_field :password, class: 'box1 spree-flat-input', placeholder: Spree.t(:password) %>
          </p>
        </div>
      </div>

      <% if Spree::Frontend::Config[:remember_me_enabled] %>
        <div class="spree-checkbox-container mt-4 mt-sm-5 mt-lg-4 spree-mb-medium">
          <div class="spree-checkbox">
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me, '' %>
          </div>

          <%#= f.label :remember_me, Spree.t(:remember_me), class: 'spree-checkbox-label' %>
        </div>
      <% end %>

      <%= f.submit Spree.t(:log_in), class: 'btn btn-primary btn-block spree-btn mt-2' %>
    <% end %>

    <% if spree.respond_to?(:recover_password_path) %>
      <div class="text-center forgot-password mb-4">
        <%= link_to Spree.t(:forgot_password), spree.recover_password_path, class: 'fp' %>
      </div>
    <% end %>
    
  </div>
  <%= render partial: 'spree/shared/copyright' %>
</div>

<style type="text/css">
  @import url('https://fonts.googleapis.com/css?family=Rubik&display=swap');
  @import url('https://fonts.googleapis.com/css?family=Rubik+Mono+One&display=swap');
  @import url('https://fonts.googleapis.com/css?family=Rubik+Microbe&display=swap');
  @import url('https://fonts.googleapis.com/css?family=Rubik+Glitch&display=swap');
  @import url('https://fonts.googleapis.com/css?family=Rubik+Wet+Paint&display=swap');
  @import url('https://fonts.googleapis.com/css?family=Rubik+Bubbles&display=swap');
  @import url('https://fonts.googleapis.com/css?family=Rubik+Puddles&display=swap');

  body, html {
    margin: 0;
    padding: 0;
    width: 100%;
  }

  html, body {
    margin: 0 !important;
    padding: 0 !important;
    height: 100% !important;
    overflow: hidden !important;
  }

  #content {
    padding: 0 !important;
    margin: 0 !important;
  }

  .divclass {
    font-family: "Open Sans", sans-serif;
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    margin: 0;
    padding: 0;
  }

  @keyframes spinner {
    0% { transform: rotateZ(0deg); }
    100% { transform: rotateZ(359deg); }
  }

  * {
    box-sizing: border-box;
  }

  .wrapper {
    display: flex;
    align-items: center;
    flex-direction: column;
    justify-content: center;
    width: 100vw;
    height: 100vh;
    min-height: 100vh;
    padding: 20px;
    margin: 0;
    background: rgba(33, 150, 243, 0.85);
    font-family: "Open Sans", sans-serif;
    background-image: url("https://i.imgur.com/yzS1Nse.jpeg");
    background-size: cover;
    background-position: center center;
    background-repeat: no-repeat;
    background-attachment: fixed;
  }

  .footer-spree-copyright {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    width: 100%;
    z-index: 2;
    background: rgba(0, 0, 0, 0.5);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    text-align: center;
  }

  .new_spree_user {
    border-radius: 2px 2px 5px 5px;
    padding: 10px 20px 20px 20px;
    max-width: 320px;
    background: #ffffff;
    position: relative;
    padding-bottom: 80px;
    box-shadow: 0px 1px 5px rgba(0,0,0,0.3);
  }
    &.loading {
      .btn {
        max-height: 100%;
        padding-top: 50px;
        .spinner {
          opacity: 1;
          top: 40%;
        }
      }
    }

    &.ok {
      .btn {
        background-color: #8bc34a;
      }
    }

    .fp {
     font-size: 0.9em;
      color: aliceblue;
      text-decoration: none;
    }

    .box1 {
      display: block;
      padding: 15px 10px;
      margin-bottom: 10px;
      width: 100%;
      border: 1px solid #ddd;
      transition: border-width 0.2s ease;
      border-radius: 2px;
      color: black;
  }
      &+ i.fa {
          color: #fff;
        font-size: 1em;
        position: absolute;
        margin-top: -47px;
        opacity: 0;
        left: 0;
        transition: all 0.1s ease-in;
      }

      &:focus {
        &+ i.fa {
          opacity: 1;
          left: 30px;
        transition: all 0.25s ease-out;
        }
        outline: none;
        color: #444;
        border-color: #2196F3;
        border-left-width: 35px;
      }


    .title {
      color: #444;
      font-size: 1.2em;
      font-weight: bold;
      margin: 10px 0 30px 0;
      border-bottom: 1px solid #eee;
      padding-bottom: 20px;
    }

    .btn {
      width: 100%;
      height: 100%;
      padding: 10px 10px;
      background: #2196F3;
      color: black;
      display: block;
      border: none;
      margin-top: 20px;
      position: absolute;
      left: 0;
      bottom: 0;
      max-height: 60px;
      border: 0px solid rgba(0,0,0,0.1);
    border-radius: 0 0 2px 2px;
      transform: rotateZ(0deg);

      transition: all 0.1s ease-out;
        border-bottom-width: 7px;
      }

      .spinner {
        display: block;
        width: 40px;
        height: 40px;
        position: absolute;
        border: 4px solid #ffffff;
        border-top-color: rgba(255,255,255,0.3);
        border-radius: 100%;
        left: 50%;
        top: 0;
        opacity: 0;
        margin-left: -20px;
        margin-top: -20px;
        animation: spinner 0.6s infinite linear;
        transition: top 0.3s 0.3s ease,
                  opacity 0.3s 0.3s ease,
                  border-radius 0.3s ease;
        box-shadow: 0px 1px 0px rgba(0,0,0,0.2);
      }



      &:not(.loading) .btn:hover {
        box-shadow: 0px 1px 3px #2196F3;
      }
      &:not(.loading) .btn:focus {
        border-bottom-width: 4px;
      }




  footer {
    display: block;
    padding-top: 50px;
    text-align: center;
    color: #ddd;
    font-weight: normal;
    text-shadow: 0px -1px 0px rgba(0,0,0,0.2);
    font-size: 0.8em;
    a, a:link {
      color: #fff;
      text-decoration: none;
    }
  }


</style>
